{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main EXOCORTEX window; no shell, filesystem, or external URL permissions granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
